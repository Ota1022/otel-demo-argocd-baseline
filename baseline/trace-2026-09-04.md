# Baseline trace (local Jaeger) — 2026-09-04

Fill this in after running the steps in README "Distributed trace verification".
Values in angle brackets are placeholders.

- Chart: opentelemetry-demo 0.41.0 (appVersion 3.0.0)
- Collector: otel/opentelemetry-collector-contrib, chart default pipelines, no changes
- Backends: Jaeger (in-memory) / Prometheus / Grafana / OpenSearch

## Trace 1: checkout

- Trace ID: `<32 hex>`
- How it was produced: `<browser "Place Order" | k6 load generator>`
- Span flow (rewrite to match the real parent/child chain):
  `frontend-web -> frontend-proxy -> frontend -> checkout -> cart / currency / payment / shipping -> quote / email / product-catalog -> kafka -> accounting / fraud-detection`
- service.name values seen: `<list>`
- Resource attributes (representative): `service.name`, `service.version=3.0.0`, `service.namespace=opentelemetry-demo`,
  `service.instance.id` (from k8s.pod.uid), `k8s.pod.name`, `k8s.namespace.name`, `k8s.node.name`, `host.name`, `service.criticality`
- Span attributes (representative): `http.route`, `http.request.method` (or `http.method`, record what is actually present),
  `rpc.system`, `rpc.service`, `rpc.method`, `demo.order.id`, `demo.cart.*`, `demo.payment.*`, `demo.shipping.*`
- How it looks in Jaeger: `<span count, total duration, number of services, errors>`

## Grafana

- Dashboard(s) opened: `<name>`
- Panels that reflect the trace above: `<list>`
